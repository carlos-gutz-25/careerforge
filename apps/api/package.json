{
  "name": "@careerforge-app/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=../../.env src/main.ts",
    "profile:import": "node --env-file-if-exists=../../.env src/cli/import-profile.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@careerforge/core": "workspace:*",
    "@careerforge/db": "workspace:*",
    "@fastify/cookie": "^11.1.1",
    "@node-rs/argon2": "^2.0.2",
    "fastify": "^5.0.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@careerforge/config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
