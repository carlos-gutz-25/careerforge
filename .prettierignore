# Prose and planning docs are hand-formatted (mermaid, tables); prettier stays out.
docs/
*.md

# CI/workflow yaml predates the formatter; formatted files only going forward.
.github/

# Per-seat agent config (untracked settings.local.json and friends) is local tooling, not repo source.
.claude/

pnpm-lock.yaml
coverage/
dist/
.output/
.nuxt/
test-results/
playwright-report/
